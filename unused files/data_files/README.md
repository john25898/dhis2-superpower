# Backend

This folder holds the DHIS export pipeline and backend-specific helpers.

- `dhis_export.py`: exports metadata and analytics CSVs into `data/dhis/`
- Keep DHIS credentials in environment variables, not in source files.
