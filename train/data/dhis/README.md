# DHIS Export Layout

- `raw/`: direct CSV exports from DHIS analytics endpoints
- `meta/`: metadata CSVs such as data elements, indicators, and organisation units
- `processed/`: merged or cleaned datasets used by the dashboard
