"""Per-project packages for the CHAK VISTA dashboard.

Each project lives in its own folder:
  - <slug>/config.js  — project config (datasets, dashboards, visualizations)
  - <slug>/backend.py — project-specific Flask blueprint (optional)
  - <slug>/*.csv      — project data files (optional)
"""
